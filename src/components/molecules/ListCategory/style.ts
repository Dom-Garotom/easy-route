import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    componente:{
        height:35,
        maxHeight: 35,
        marginVertical: 10,
        marginBottom:10,
        position:"absolute",
        top: -55
    },
    container : {
        gap: 10,
        paddingHorizontal: 8,
        overflow:"hidden"
    }
})